/**
 * reports/profit-loss.tsx
 * Re-exports the canonical P&L screen from finance/ so the reports hub
 * can deep-link to ./profit-loss without duplicating logic.
 */
export { default } from '@/app/(app)/finance/profit-loss';
